rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // รูปสินค้า — เจ้าของร้านเขียน ลูกค้าอ่านได้
    match /shops/{shopId}/products/{file} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == shopId;
    }
  }
}
